# KNOWN_LIMITATIONS.md

Deferred-work ledger (§10.2 of PLAN.md). Every `TODO(review) KL-NN` marker in code
maps to exactly one live row here; every non-pre-declared row maps to exactly one
marker. Struck-through rows are historical (closing commit in the rationale).

| KL-NN | Title | User-visible | Pre-declared | Rationale |
|---|---|---|---|---|
| KL-01 | single-process, single-run-at-a-time agent | user-visible: yes | pre-declared: yes | One agent process at a time; a second concurrent run is rejected by RunRegistry (marker TODO(review) KL-01 at RunRegistry; 409). Concurrent runs / distributed execution are out of scope (§12). |
| KL-02 | no authentication on the control API | user-visible: yes | pre-declared: yes | Sandbox containers are the security boundary; API is demo posture. Auth/multi-tenant are out of scope (§12). |
| KL-03 | Maven-only fixture scope (D4) | user-visible: yes | pre-declared: yes | Gradle / Kotlin-DSL fixture builds, non-Maven targets, and multi-module reactor targets are explicitly out of scope; fixtures are plain Maven projects (D4, §12). |
| KL-04 | LLM diagnoses are advisory | user-visible: yes | pre-declared: yes | A BuildDiagnosis is guidance for the planner; correctness is asserted only by deterministic build/test outcomes, never model confidence (§12). |
| KL-05 | eval N is small (4 fixtures) | user-visible: yes | pre-declared: yes | The fixture set is a smoke signal, not a benchmark; no statistical rigor is claimed (§12). |
| KL-06 | Ollama path may be slow on modest hardware | user-visible: yes | pre-declared: yes | Local-model latency influences timeouts; the live-eval threshold applies to whichever provider is configured (§12). |
| KL-07 | JVM reflection can construct Validated* in-process | user-visible: no | pre-declared: yes | The executor boundary defends the LLM/planner path, not malicious in-process code: reflection can build ValidatedPatch (marker TODO(review) KL-07 at UpgradeExecutor; ExecutorBoundaryTest proves the digest check still refuses such instances). |
| KL-08 | the resume re-enters at the last apply; nothing mid-flight is snapshotted | user-visible: yes | pre-declared: yes | The snapshot is the validated plan payload (+ pending patch), not the machine's in-flight state: the sandbox copy, BuildResult and Repairing diagnostic die with the JVM and are re-derived (the build re-runs; the diagnosis is re-made). Frames before the first apply have no payload and are not resumable (§13.3 deviation, phase-4 report: the plan's early-termination-cut framing was replaced by a real kill -9 demonstration, and the Applying-only frame gate by the payload re-seed; marker TODO(review) KL-08 at RunSnapshot.kt). |
| KL-09 | no programmatic WaitFor submission in Embabel 1.5.1 — the C-6 fallback is the pattern | user-visible: no | pre-declared: yes | The historical submitFormAndResumeProcess/_confirm was removed from the release line (github.com/embabel/embabel-agent#1447: present in 0.3.2, absent in 0.3.3+); 1.5.1 has no public pending-awaitable accessor (jar-verified). The documented fallback (blackboard decision + re-seed) is implemented and proven end-to-end (ApprovalGateIT: park -> POST decision -> continuation completes; marker TODO(review) KL-09 at RunService.submitDecision). |
| KL-10 | binary/rename/deletion diffs rejected by scope (L2) | user-visible: yes | pre-declared: yes | Layer 2 rejects binary diffs, rename-only diffs, and deletion diffs by scope (PLAN §12 / §10.2 pre-declared; marker TODO(review) KL-10 at DiffApplyValidator). The planner replans around the rejection instead of patching binaries. |
| ~~KL-12~~ | LLM client retries non-retryable quota errors | user-visible: no | CLOSED (phase-3.2 commit) | Implemented: `agent/llm/LlmCall.kt` (LlmRetryClassifier taxonomy: 429-quota/401/400/404 → fail fast; 429-rate/5xx/transport → bounded backoff) + framework data-binding max-attempts pinned to 1 (application.yml). Closed by phase-3.2; `LlmRetrySemanticsTest` proves red (quota → 1 attempt) and transient (retried). |
