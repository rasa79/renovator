{
  "filePath": "src/main/java/com/example/removal/EscapeSqlFormatter.java",
  "unifiedDiff": "--- a/src/main/java/com/example/removal/EscapeSqlFormatter.java\n+++ b/src/main/java/com/example/removal/EscapeSqlFormatter.java\n@@ -1,11 +1,12 @@\n package com.example.removal;\n \n-import org.apache.commons.lang.StringEscapeUtils;\n-\n /**\n  * Uses exactly one commons-lang 2.6 API that has NO equivalent in commons-lang3:\n  * `StringEscapeUtils.escapeSql` (SQL-escaping was removed from the lang3 family\n  * entirely). The upgrade therefore fails to compile with a \"cannot find symbol\"\n  * error naming `escapeSql` — the signal the planner consumes (see README.md).\n+ *\n+ * REPAIRED: escapeSql implemented locally; the commons-lang3 family has no\n+ * SQL-escaping utility, so the call's single-quote doubling is inlined.\n  */\n public final class EscapeSqlFormatter {\n@@ -16,4 +17,4 @@\n     public static String escapeSql(String input) {\n-        return StringEscapeUtils.escapeSql(input);\n+        return input.replace(\"'\", \"''\");\n     }\n }\n",
  "justification": "repair: commons-lang3 has no escapeSql (SQL-escaping removed from the lang3 family); the call is replaced by a local single-quote doubling implementation"
}
