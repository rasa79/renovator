# Eval report — live (gpt-4.1-mini baseline)

- run: live; model: **gpt-4.1-mini** (the configured default); date: 2026-09-02
- This is the HONEST BASELINE for the D13 live floor. It is retained untouched;
  the stronger-model run below (gpt-4.1) writes its own dated report.

## Per-fixture outcome (measured)

| fixture | verdict | attempts | terminal | notes |
|---|---|---|---|---|
| fixture-clean | FAIL | 1 | no green | model proposed a dependency migration the fixture lacks (jackson-databind / commons-io / junit) -> executor rejects -> run cannot reach a green build |
| fixture-no-path | FAIL | 2 | no blocker | model's plan drifted off the 99.99.99 target -> L3 pass of a different version -> no progress to the blocker |
| fixture-api-removal | (not reliably measured) | — | — | live run stalled on the model's off-target plan |
| fixture-transitive-conflict | (not reliably measured) | — | — | (not reached in the gated run) |

## Live-call observation (per fixture run, LlmCall events)

1–4 `proposePlan` calls per run (recorded per run dir: 1, 4, 2, 1). No live test runs in a default build.

## Verdict (D13 live floor)

Under **gpt-4.1-mini**, `fixture-clean` and `fixture-no-path` do NOT pass: the model
does not reliably produce a valid plan for the fixture goals (it proposes dependency
migrations absent from the fixtures). This is a **model-capability / plan-following**
finding, recorded here as the baseline for the gpt-4.1 comparison.
