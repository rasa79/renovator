<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- fixture-transitive-conflict (PLAN §8.3): the single-hop-fails fixture.
         DRIFT absorbed per §13.3 (environment note, phase-1 report): guice-parent
         7.0.0 transitively pins guava 31.0.1-jre (verified from the guice-parent
         pom; the plan's assumed 32.1.2-jre pin does not exist in any 6.x/7.x
         guice release). Per §8.3 set C, pairs are replaceable while the MECHANISM
         holds: fixed B (guice) transitively pins A (guava) at a version != the
         upgrade target. Coordinates: direct guava 31.0.1-jre -> 33.4.8-jre,
         fixed guice 7.0.0. -->
    <groupId>com.example.conflict</groupId>
    <artifactId>fixture-transitive-conflict</artifactId>
    <version>1.0.0</version>

    <properties>
        <maven.compiler.release>17</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <junit.version>5.11.4</junit.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>com.google.guava</groupId>
            <artifactId>guava</artifactId>
            <version>31.0.1-jre</version>
        </dependency>
        <!-- Fixed on purpose: must NOT change as part of the goal. -->
        <dependency>
            <groupId>com.google.inject</groupId>
            <artifactId>guice</artifactId>
            <version>7.0.0</version>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.5.6</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <version>3.6.3</version>
                <dependencies>
                    <dependency>
                        <groupId>org.apache.maven.enforcer</groupId>
                        <artifactId>enforcer-rules</artifactId>
                        <version>3.6.3</version>
                    </dependency>
                </dependencies>
                <executions>
                    <execution>
                        <id>enforce-convergence</id>
                        <phase>validate</phase>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <dependencyConvergence />
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
